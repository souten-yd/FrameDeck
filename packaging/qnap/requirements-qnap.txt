# Runtime-only dependencies bundled into the QPKG.
# Keep this list architecture-neutral and prefer manylinux wheels so QPKG builds
# do not require a compiler. Pin the web stack to the FrameDeck-tested series so
# future Starlette/FastAPI API changes do not silently alter a released QPKG.
Pillow>=10,<13
rarfile>=4,<5
fastapi>=0.139,<0.140
uvicorn>=0.40,<0.53
aiofiles>=24,<26
pydantic>=2.8,<3
send2trash>=1.8,<2
