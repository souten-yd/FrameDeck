# Runtime-only dependencies bundled into the QPKG.
# Keep this list architecture-neutral and prefer manylinux wheels so QPKG builds
# do not require a compiler.
Pillow>=10,<13
rarfile>=4,<5
fastapi>=0.115,<1
uvicorn>=0.30,<1
aiofiles>=24,<26
pydantic>=2.8,<3
send2trash>=1.8,<2
